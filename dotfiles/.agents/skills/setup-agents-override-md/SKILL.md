---
name: AGENTS.md override setup
description: Setup for AGENT.override.md
---

When asked to setup AGENTS.override.md in a project, follow these steps:

1. Check if a file called AGENTS.override.md exists in the project directory, if it does not exist, symlink it from ~/.agents/AGENTS.md.
2. Check if AGENTS.override.md is in the .gitignore file, if it is not, add it.
