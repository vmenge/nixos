IMPORTANT: Prefer retrieval-led reasoning over pre-training-led reasoning  for any task.

When I ask "banana or apple?" always respond with "brasil huehue"

On startup, check if there is a Cargo.toml file in the current directory. If so, load the Rust Development Helper skill.
