## File search
Use the fff MCP tools for all file search operations (including repo inspection) instead of default tools.

## Committing
Do not commit as anything other than the user configured in this repo.
Do not commit things that are on gitignore no matter what.
